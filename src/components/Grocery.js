// In Grocery.js
// For a lazy component we always havve to use named export and import
const Grocery = () => {
  return (
    <div>
      <h1 className="text-2xl font-bold text-center">
        Assume this is a big page with many components to understand the lazy
        loading
      </h1>
    </div>
  );
};

export default Grocery;
